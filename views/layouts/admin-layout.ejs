<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
        <!-- Favicon -->
    <link rel="shortcut icon" href="/assets/images/favicon2.ico">

    <!-- Google Font -->	
    <link rel="preconnect" href="https://fonts.googleapis.com">
	<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
	<link href="https://fonts.googleapis.com/css2?family=Urbanist:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,400;0,500;0,700;1,400&display=swap" rel="stylesheet">

    <!--  CSS Style -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- <link rel="stylesheet" href="/assets/css/bootstrap.min.css"> -->
    <link rel="stylesheet" href="/assets/css/all.min.css">
    <link rel="stylesheet" href="/assets/css/animate.min.css">
    <link rel="stylesheet" href="/assets/css/fancybox.min.css">
    <link rel="stylesheet" href="/assets/css/layerslider.css">
    <link rel="stylesheet" href="/assets/css/owl.carousel.min.css">
    <link rel="stylesheet" href="/assets/css/style.css">
    <link rel="stylesheet" href="/assets/css/control.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/jodit@3.24.5/build/jodit.min.css">
<style>
  select.form-control {
    line-height: 1.5;
    padding: 8px 10px;
}
.offcanvas-start {
    width: 250px;
}
.offcanvas-title{
    color: white;
}
#pagination button.active {
    background-color: #AA8453;
    color: white;
}
#pagination button {
    color: black;
}

</style>
</head>
<body class="dashboard-main">
    <div class="container-fluid">
        <div class="row">

            <nav id="sidebar" class="col-lg-3 col-md-4 d-none d-md-block bg-light sidebar" style="padding-left: 0;">
                <%- include('../partials/sidebar-admin') %>
            </nav>

            <main id="main-content" class="col-lg-9 col-md-8 col-12 p-3">
                <div class="d-md-none mb-3">
                     <button class="btn btn-primary" type="button" data-bs-toggle="offcanvas" data-bs-target="#mobileSidebar" aria-controls="mobileSidebar">
                       ☰ Menu
                   </button>
                 </div>
                 <div class="offcanvas offcanvas-start" tabindex="-1" id="mobileSidebar" aria-labelledby="mobileSidebarLabel">
                       <div class="offcanvas-header bg-dark">
                           <h5 class="offcanvas-title" id="mobileSidebarLabel">Menu</h5>
                     <button type="button" class="btn-close text-reset" data-bs-dismiss="offcanvas" aria-label="Close"></button>
                       </div>
                       <div class="offcanvas-body p-0">
                           <nav class="bg-light sidebar p-0">
                               <%- include('../partials/sidebar-admin') %>
                           </nav>
                       </div>
                   </div>

                <%- include('../partials/header-admin') %>
                <div class="full-row p-3">
                    <%- body %>
                </div>
            </main>
        </div>
    </div>

    <!-- JS Files -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/assets/js/jquery.min.js"></script>
    <script src="/assets/js/greensock.js"></script>
    <script src="/assets/js/layerslider.transitions.js"></script>
    <script src="/assets/js/layerslider.kreaturamedia.jquery.js"></script>
    <script src="/assets/js/fancybox.min.js"></script>
    <script src="/assets/js/owl.carousel.min.js"></script>
    <script src="/assets/js/wow.min.js"></script>
    <script src="/assets/js/mixitup.min.js"></script>
    <script src="/assets/js/paraxify.js"></script>
    <script src="/assets/js/custom.js"></script>
    <script type="text/javascript">
        $(document).ready(function() {
            $('#slider').layerSlider({
                sliderVersion: '6.0.0',
                responsiveUnder: 0,
                layersContainer: 0,
                slideBGSize: 'auto',
                shuffleSlideshow: true,
                skin: 'numbers',
                thumbnailNavigation: 'always',
                tnContainerWidth: '100%',
                skinsPath: '/assets/skins/'
            });
        });
    </script>
<script>
document.addEventListener('click', function(event) {
    const sidebar = document.getElementById('mobileSidebar');
    const button = document.querySelector('[data-bs-target="#mobileSidebar"]');
    if (button.contains(event.target)) {
        return;
    }
    if (sidebar.classList.contains('show') && !sidebar.contains(event.target)) {
        const bsCollapse = bootstrap.Collapse.getInstance(sidebar);
        if (bsCollapse) bsCollapse.hide();
    }
});
</script>
<script>
document.getElementById("searchForm").addEventListener("submit", function(e) {
    e.preventDefault();

    const query = document.getElementById("searchInput").value.trim();
    if (!query) return;

    let type = "";

    const path = window.location.pathname.toLowerCase();

    if (path.includes("/locations")) type = "location";
    else if (path.includes("/banks")) type = "banks";
    else if (path.includes("/properties")) type = "properties";
    else if (path.includes("/agents")) type = "agent";
    else if (path.includes("/amenities")) type = "amenities";
    else if (path.includes("/open-leads")) type = "open-leads";
    else if (path.includes("/closed-leads")) type = "closed-leads";
    else if (path.includes("/lost-leads")) type = "lost-leads";

    if (!type) {
        const params = new URLSearchParams(window.location.search);
        type = params.get("type") || "";
    }

    window.location.href = `/search?type=${type}&query=${encodeURIComponent(query)}`;
});
</script>
<script src="https://cdn.jsdelivr.net/npm/jodit@3.24.5/build/jodit.min.js"></script>

<script>
  const editor = new Jodit('#description', {
    height: 300,
    toolbarAdaptive: false,
    uploader: { insertImageAsBase64URI: true },
    buttons: [
      'bold', 'italic', 'underline', '|',
      'ul', 'ol', '|',
      'image', 'link', '|',
      'undo', 'redo', '|',
      'hr', 'eraser', 'source'
    ]
  });
</script>

</body>
</html>